apiVersion: apps/v1
kind: Deployment
metadata:
  name: bot-bezrealitky
  namespace: dp-reality
  labels:
    app.kubernetes.io/part-of: dp-reality
    app.kubernetes.io/component: bot
    app.kubernetes.io/name: bot-bezrealitky
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: bot-bezrealitky
  template:
    metadata:
      labels:
        app.kubernetes.io/part-of: dp-reality
        app.kubernetes.io/component: bot
        app.kubernetes.io/name: bot-bezrealitky
    spec:
      containers:
        - name: bot-bezrealitky
          image: ghcr.io/ryxwaer/dp-reality/bot-bezrealitky:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef: { name: bot-bezrealitky }
            - secretRef:    { name: bot-bezrealitky }
          ports:
            - { name: http, containerPort: 8000 }
          readinessProbe:
            httpGet: { path: /healthz, port: 8000 }
            initialDelaySeconds: 5
            periodSeconds: 15
          livenessProbe:
            httpGet: { path: /healthz, port: 8000 }
            initialDelaySeconds: 30
            periodSeconds: 30
