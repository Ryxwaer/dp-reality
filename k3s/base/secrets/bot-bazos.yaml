# Placeholder Secret. The operator overwrites the values in-cluster
# via `kubectl create secret generic ... --dry-run=client -o yaml |
# kubectl apply -f -` per the runbook step 4. Phase 3 replaces this
# with a SOPS-encrypted manifest.
apiVersion: v1
kind: Secret
metadata:
  name: bot-bazos
  namespace: dp-reality
  labels:
    app.kubernetes.io/part-of: dp-reality
    app.kubernetes.io/component: bot
    app.kubernetes.io/name: bot-bazos
type: Opaque
stringData:
  MONGODB_URI: REPLACE_ME
  RABBITMQ_URL: REPLACE_ME
