apiVersion: v1
kind: ConfigMap
metadata:
  name: bot-bezrealitky
  namespace: dp-reality
  labels:
    app.kubernetes.io/part-of: dp-reality
    app.kubernetes.io/component: bot
    app.kubernetes.io/name: bot-bezrealitky
data:
  SERVICE_ID: bot-bezrealitky
  DISPLAY_NAME: Bezrealitky
  BASE_URL: http://bot-bezrealitky:8000
  SCRAPE_INTERVAL_MINUTES: "10"
  # Anti-bot countermeasures (NFR-01-B): per-page throttle, post-block
  # backoff, per-cycle detail-fetch cap.
  SCRAPE_THROTTLE_SECONDS_BETWEEN_PAGES: "2.0"
  BACKOFF_MINUTES_ON_BLOCK: "30"
  MAX_DETAIL_FETCHES_PER_CYCLE: "60"
  GEO_SEED_MODE: missing
  # Required by Nominatim's usage policy — keep the contact in there.
  GEO_USER_AGENT: "dp-reality-bot-bezrealitky/1.0 (https://github.com/ryxwaer/dp-reality)"
