motor==3.7.1
aio-pika==9.6.2
httpx==0.28.1
pydantic-settings==2.14.1
pydantic==2.13.4
apscheduler==3.11.2
fastapi==0.136.1
uvicorn[standard]==0.47.0
tenacity==9.1.4
shapely==2.1.2
pyproj==3.7.2

# OpenTelemetry — emit OTLP/gRPC spans to Tempo. The aio-pika
# instrumentation auto-injects W3C traceparent on publish so the
# email-notifier's manual extraction continues the same trace; the
# pymongo instrumentation covers Motor (Motor wraps PyMongo); the
# httpx + fastapi instrumentations cover both directions of HTTP
# traffic between the BFF and this bot. From 0.50b0 onwards the HTTP
# instrumentations emit the new (stable) HTTP semantic conventions
# by default — `server.address`, `http.request.method`, `url.full`
# — which is what Tempo's `service_graphs` processor uses to label
# outbound peers.
opentelemetry-sdk==1.41.1
opentelemetry-exporter-otlp-proto-grpc==1.41.1
opentelemetry-instrumentation-aio-pika==0.62b1
opentelemetry-instrumentation-pymongo==0.62b1
opentelemetry-instrumentation-httpx==0.62b1
opentelemetry-instrumentation-fastapi==0.62b1
